# ============== API Gateway ==============
PORT=3000
# URI de MongoDB (local o Atlas). Variable estándar para el api-gateway.
MONGO_URI=mongodb://localhost:27017/el_patio_db
TELEGRAM_BOT_TOKEN=
JWT_SECRET=

# TON / Telegram Stars (si aplica)
# COINGECKO_API_URL=https://api.coingecko.com

# ============== Game Server ==============
# PORT=3001  (en .env del game-server o aquí si compartido)
API_GATEWAY_URL=http://localhost:3000

# ============== Bot Telegram ==============
# BOT_TOKEN= (o TELEGRAM_BOT_TOKEN según cómo lo inyectes en Docker)
WEBAPP_URL=https://tu-webapp.vercel.app
API_GATEWAY_URL=http://localhost:3000

# ============== Webapp (Vite - usar en apps/webapp/.env) ==============
# Local: suele ser http://localhost:3000 cuando el api-gateway corre en Docker/local.
# Vercel: URL pública del API (p. ej. https://el-patio-api.fly.dev si usas Fly.io).
# VITE_API_URL=http://localhost:3000
# VITE_WS_URL=http://localhost:3001

# ============== Dashboard (Vite - usar en apps/dashboard/.env) ==============
# VITE_API_URL=http://localhost:3000

# ============== Producción / Fly.io ==============
# Para el api-gateway en Fly.io: configurar MONGO_URI, JWT_SECRET, TELEGRAM_BOT_TOKEN,
# CORS_ORIGIN (incluir URL de la webapp en Vercel si aplica) vía fly secrets set.

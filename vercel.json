{
  "buildCommand": "if [ -d apps/webapp ]; then npm run build -w webapp; else (cd ../.. && npm run build -w webapp); fi",
  "outputDirectory": "apps/webapp/dist",
  "framework": "vite",
  "installCommand": "if [ -d apps/webapp ]; then npm install; else (cd ../.. && npm install); fi",
  "rewrites": [
    { "source": "/(.*)", "destination": "/index.html" }
  ]
}
